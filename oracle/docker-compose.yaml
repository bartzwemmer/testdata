services:
  oracle:
    profiles:
      - oracle
    image: container-registry.oracle.com/database/free:23.26.1.0-lite
    container_name: oracle
    ports:
      - "1521:1521"
    environment:
      - ORACLE_PASSWORD=${ORACLE_PASSWORD}
      - ORACLE_PDB=pdb1
      - ORACLE_CHARACTERSET=AL32UTF8
    volumes:
      - oracle_data:/opt/oracle/oradata
      - ./scripts:/opt/oracle/scripts
    deploy:
      resources:
        limits:
          cpus: '2.0'
          memory: 4gb
    healthcheck:
      test: ["CMD-SHELL", "/opt/oracle/checkDBStatus.sh"]
      interval: 30s
      timeout: 10s
      retries: 10
    networks:
      - oracle_net

  init-oracle:
    image: alpine:latest
    container_name: init-oracle
    profiles:
      - oracle
    depends_on:
      oracle:
        condition: service_healthy
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ./scripts:/opt/oracle/scripts
    entrypoint: [ "sh", "-c", "apk add --no-cache docker-cli && docker exec oracle sqlplus -s / as sysdba @/opt/oracle/scripts/setup.sql ${ORACLE_PASSWORD} \"\" YES" ]
    networks:
      - oracle_net

volumes:
  oracle_data:
  oracle_temp:


networks:
  oracle_net:
    driver: bridge
